### SysChangeGroupType

```cfscript
var groupId = "a-group-id";
var groupType = "group-type";
var groupProxy = bridge.getGroupServiceProxy();

var postResult = groupProxy.sysChangeGroupType(groupId, groupType);
if (postResult.status == 200) {
    // Success!
}
```
```cpp
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```csharp
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```java
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```javascript
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```objective_c
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```r
{
	"service": "group",
	"operation": "SYS_CHANGE_GROUP_TYPE",
	"data": {
		"groupId": "a-group-id",
		"groupType": "group-type"
	}
}
```

<%= partial "partials/jsonBtn" %>

```json-doc
{
  "data": null,
  "status": 200
}
```

<%= partial(:"partials/versionTag", :locals => {
  :version => "4.1.0",
  :newText => "",
  :comingText => ""
}) %>

Changes the Group Type.

<%= partial(:"partials/servop", :locals => { :service_name => "group", :operation_name => "SYS_CHANGE_GROUP_TYPE" }) %>

#### Method Parameters
Parameter | Description
--------- | -----------
groupId | ID of the group 
groupType | Group Type 

