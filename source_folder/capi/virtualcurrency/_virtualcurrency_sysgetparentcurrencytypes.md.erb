### SysGetParentCurrencyTypes

```cfscript
var virtualCurrencyProxy = bridge.getVirtualCurrencyServiceProxy();

var postResult = virtualCurrencyProxy.sysGetParentCurrencyTypes();
if (postResult.status == 200) {
    // Success!
}
```
```cpp
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```csharp
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```java
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```javascript
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```objective_c
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```r
{
	"service": "virtualCurrency",
	"operation": "SYS_GET_PARENT_CURRENCY_TYPES",
	"data": {}
}
```

<%= partial "partials/jsonBtn" %>

```json-doc
{
  "data": {
    "parentCurrencies": {
      "parent1": [
        "parent1VC2",
        "parent1VC1"
      ],
      "parent2": [
        "parent2VC1",
        "parent2VC2"
      ]
    }
  },
  "status": 200
}
```
<%= partial(:"partials/versionTag", :locals => {
  :version => "4.5.5",
  :newText => "",
  :comingText => ""
}) %>

Retrieves the virtual currency types inherited from the parent app(s).

<%= partial(:"partials/servop", :locals => { :service_name => "virtualCurrency", :operation_name => "SYS_GET_PARENT_CURRENCY_TYPES" }) %>
