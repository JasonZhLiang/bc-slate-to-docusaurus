### SysGetPeerCurrencyTypes

```cfscript
var virtualCurrencyProxy = bridge.getVirtualCurrencyServiceProxy();

var postResult = virtualCurrencyProxy.sysGetPeerCurrencyTypes();
if (postResult.status == 200) {
    // Success!
}
```
```cpp
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```csharp
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```java
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```javascript
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```objective_c
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```r
{
	"service": "virtualCurrency",
	"operation": "SYS_GET_PEER_CURRENCY_TYPES",
	"data": {}
}
```

<%= partial "partials/jsonBtn" %>

```json-doc
{
  "data": {
    "peerCurrencies": {
      "deploypeer": [
        "gems"
      ]
    }
  },
  "status": 200
}
```
<%= partial(:"partials/versionTag", :locals => {
  :version => "4.5.5",
  :newText => "",
  :comingText => ""
}) %>

Retrieves the virtual currency types inherited from the configured peer apps.

<%= partial(:"partials/servop", :locals => { :service_name => "virtualCurrency", :operation_name => "SYS_GET_PEER_CURRENCY_TYPES" }) %>
