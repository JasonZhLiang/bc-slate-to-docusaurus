### SysGetCurrencyTypes

```cfscript
var virtualCurrencyProxy = bridge.getVirtualCurrencyServiceProxy();

var postResult = virtualCurrencyProxy.sysGetCurrencyTypes();
if (postResult.status == 200) {
    // Success!
}
```
```cpp
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```csharp
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```java
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```javascript
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```objective_c
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```r
{
	"service": "virtualCurrency",
	"operation": "SYS_GET_CURRENCY_TYPES",
	"data": {}
}
```

<%= partial "partials/jsonBtn" %>

```json-doc
{
    "data": {
        "currencies": [
          "gems"
        ]
      },
    "status": 200
}
```
<%= partial(:"partials/versionTag", :locals => {
  :version => "4.5.5",
  :newText => "",
  :comingText => ""
}) %>

Retrieves the virtual currency types defined for the current app.

<%= partial(:"partials/servop", :locals => { :service_name => "virtualCurrency", :operation_name => "SYS_GET_CURRENCY_TYPES" }) %>
