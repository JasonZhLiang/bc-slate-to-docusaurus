### SysCancelScheduledScriptIfExists

```cfscript
var jobId = "idsfihihfuvhvuh";
var scriptProxy = bridge.getScriptServiceProxy();

var postResult = scriptProxy.sysCancelScheduledScriptIfExists(jobId);
if (postResult.status == 200) {
  // Success!
}
```
```cpp
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```csharp
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```java
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```javascript
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```objective_c
// Cloud Code only. To view example, switch to the Cloud Code tab
```
```r
{
  "service": "script",
  "operation": "SYS_CANCEL_SCHEDULED_SCRIPT_IF_EXISTS",
  "data":
  {
    "jobId": "idsfihihfuvhvuh"
  }
}
```

<%= partial "partials/jsonBtn" %>
```json-doc
{
  "data": {
    "cancelled": 1
  },
  "apiCount": 1,
  "totalCount": 0,
  "status": 200
}
```

<%= partial(:"partials/versionTag", :locals => {
  :version => "4.13.0",
  :newText => "",
  :comingText => ""
}) %>

Cancels a previously scheduled script, if it exists and is still in Scheduled state.

<%= partial(:"partials/servop", :locals => {  :service_name => "script", :operation_name => "SYS_CANCEL_SCHEDULED_SCRIPT_IF_EXISTS" }) %>
#### Method Parameters
Parameter | Description
--------- | -----------
jobId | The id of the job to cancel.