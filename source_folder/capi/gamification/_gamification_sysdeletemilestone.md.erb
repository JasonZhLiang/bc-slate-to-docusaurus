### SysDeleteMilestone

```r
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```objective_c
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```cpp
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```csharp
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```java
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```javascript
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```cfscript
var milestoneId = "15";

var gamificationProxy = bridge.getGamificationServiceProxy();

// Delete the milestone.
var postResult = gamificationProxy.sysDeleteMilestone(milestoneId, -1);
if (postResult.status == 200) {
    // Success!
}
```

<%= partial "partials/jsonBtn" %>

```json-doc
{
  "status": 200,
  "data": null
}
```

<%= partial(:"partials/versionTag", :locals => {
  :version => "4.3.5",
  :newText => "",
  :comingText => ""
}) %>

Deletes the specified milestone.

<%= partial(:"partials/servop", :locals => { :service_name => "gamification", :operation_name => "SYS_DELETE_MILESTONE" }) %>

#### Method Parameters
Parameter | Description
--------- | -----------
milestoneId | The id of the milestone to delete
version | The current revision of the milestone. Use -1 to bypass the version-check.


