### SysDeleteQuest

```r
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```objective_c
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```cpp
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```csharp
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```java
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```javascript
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```cfscript
var questId = "QUEST_CRUSH52";

var gamificationProxy = bridge.getGamificationServiceProxy();

// Delete the quest.
var postResult = gamificationProxy.sysDeleteQuest(questId, -1);
if (postResult.status == 200) {
    // Success!
}
```

<%= partial "partials/jsonBtn" %>

```json-doc
{
  "status": 200,
  "data": null
}
```

<%= partial(:"partials/versionTag", :locals => {
  :version => "4.3.5",
  :newText => "",
  :comingText => ""
}) %>

Deletes the specified quest.

<%= partial(:"partials/servop", :locals => { :service_name => "gamification", :operation_name => "SYS_DELETE_QUEST" }) %>

#### Method Parameters
Parameter | Description
--------- | -----------
questId | The id of the quest to delete
version | The current revision of the quest. Use -1 to bypass the version-check.


