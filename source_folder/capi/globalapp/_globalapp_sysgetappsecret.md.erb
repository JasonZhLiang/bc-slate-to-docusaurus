### SysGetAppSecret

```r
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```objective_c
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```cpp
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```csharp
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```java
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```javascript
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```cfscript
var globalAppProxy = bridge.getGlobalAppServiceProxy();

var postResult = globalAppProxy.sysGetAppSecret( );
if (postResult.status == 200) {
    // Success!
}
```

<%= partial "partials/jsonBtn" %>

```json-doc
{
  "data": {
    "secret": "883089a3-9dd0-4c53-8278-7f8ae5a9adab"
  },
  "status": 200
}
```
<%= partial(:"partials/versionTag", :locals => {
  :version => "4.8.0",
  :newText => "",
  :comingText => ""
}) %>

Retrieves app secret.

<%= partial(:"partials/servop", :locals => { :service_name => "globalApp", :operation_name => "SYS_GET_APP_SECRET" }) %>

