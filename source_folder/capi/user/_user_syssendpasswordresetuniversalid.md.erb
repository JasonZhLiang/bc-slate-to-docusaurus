### SysSendPasswordResetUniversalId

```cfscript
var userProxy = bridge.getUserServiceProxy();

var postResult = userProxy.sysSendPasswordResetUniversalId(
  "aaa-bbb-ccc-ddd"    // profileId
);

if (postResult.status == 200) {
    // Success!
}
```
```cpp
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```csharp
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```java
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```javascript
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```objective_c
// Cloud Code only. To view example, switch to the Cloud Code tab
```

```r
// Cloud Code only. To view example, switch to the Cloud Code tab
```

<%= partial "partials/jsonBtn" %>

```json-doc
{
  "status": 200,
  "data": null
}
```

<%= partial(:"partials/versionTag", :locals => {
  :version => "4.4.0",
  :newText => "",
  :comingText => ""
}) %>

Reset users password.

<%= partial(:"partials/servop", :locals => { :service_name => "user", :operation_name => "SYS_SEND_PASSWORD_RESET_UNIVERSAL_ID" }) %>

#### Method Parameters
Parameter | Description
--------- | -----------
profileId | The universal Id of the target user to reset password
